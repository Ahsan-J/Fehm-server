export enum UserStatus {
    InActive = 0,
    Active = 1,
    Blocked = 2,
}

export enum MemberShip {
    InActive = 0,
    Active = 1,
    Pro = 2,
}

export enum UserRole {
    SuperAdmin = 0,
    Admin = 1,
    User = 2,
    Narrator = 4,
}
